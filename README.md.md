Project Structure

* CandidateManagementSystem.DAL → Data Access Layer
* CandidateManagementSystem.BL → Business Logic Layer
* CandidateManagementSystem.API → API Layer
* CandidateManagement.UI → Angular Frontend



Database Schema

* Candidate (Id, Name, NickName, Email, YearsOfExperience, MaxNumSkills)
* Skill (Id, Name, GainDate, CandidateId)
